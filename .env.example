# Required for script generation and optional TTS audio.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: defaults to gpt-4.1-mini when omitted.
OPENAI_TEXT_MODEL=gpt-4.1-mini

# Optional: set to 1 to enable audio generation, or 0 to disable it.
ENABLE_TTS=0
